import React from 'react';
import { Outlet } from 'react-router-dom';

const LayoutManageOrder = () => {
    return (
        <>
            <Outlet></Outlet>
        </>
    );
};

export default LayoutManageOrder;